function Dropdown({ className }) {
  return (
    <div className={`${className} min-w-[21rem] flex justify-start px-4 py-2`}>
      <p className="text-base">Cart is Empty!</p>
    </div>
  );
}

export default Dropdown;
